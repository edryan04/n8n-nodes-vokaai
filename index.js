// n8n loads nodes + credentials directly from the `n8n` field in package.json,
// so this file is intentionally empty. It exists only because npm requires
// `main` to point at something resolvable.
module.exports = {};
